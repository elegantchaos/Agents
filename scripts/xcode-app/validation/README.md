# Validation Scripts

Project-level validation helpers for Xcode app repositories.

- `validate-changes.template.sh`: full changed-files + build validation flow
- `validate-target.template.sh`: targeted validation for one target
