This repository is expected to live at `~/.local/share/agents`.

It is the canonical source for shared agent instructions, templates, and configuration.

Each project's AGENTS.md file should direct the agent to use the contents of our `COMMON.md` as base line instructions.

Since this is itself a project, you should also use `COMMON.md` as a base line for your instructions. 