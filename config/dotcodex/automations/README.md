# Automation Templates

These templates are sanitized versions of existing local `.codex/automations/*/automation.toml` files.

Sanitization applied:

- removed `id`
- removed `created_at` / `updated_at`
- replaced machine-specific `cwds` with placeholders

Use these as starting points when creating automations on a new machine.
