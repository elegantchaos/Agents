interface:
  display_name: "Swift Validation"
  short_description: "Run standard post-change format/lint/build/test validation"
  icon_small: "./assets/validation-icon.svg"
  icon_large: "./assets/validation-icon.png"
  brand_color: "#1A1A1A"
  default_prompt: "From the repository root, run post-change formatting and validation using AgentTools: run `agt format`, then comprehensive validation with `agt validate`, then run `--target` for impacted targets, and report passes, failures, skips, and residual risks."
