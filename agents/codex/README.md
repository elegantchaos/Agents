# Codex App Notes

Guidance specific to Codex (desktop app) usage.

- Keep repository instructions in plain markdown files that can be copied into project roots.
- Prefer deterministic command-line validation and report what was actually run.
- Keep instruction files modular so context can be loaded on demand.
